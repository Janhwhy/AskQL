# GitHub personal access token — raises the unauthenticated 60 req/hr limit to 5,000/hr.
# Settings → Developer settings → Personal access tokens. No scopes needed for public repo reads.
GITHUB_TOKEN=
