# Nothing is required right now — data generation is fully simulated
# (ingestion/simulate.py), no external APIs are called. The old real-data
# ingestion (GitHub/npm/PyPI/HN) has been deleted; this file is intentionally
# near-empty to match. Future phases (the LLM agent) will add a provider API
# key here.
